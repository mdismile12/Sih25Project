# 🚀 QUICK COMMAND REFERENCE CARD

## ONE-LINER SETUP

```bash
cd c:\xampp\htdocs\update_after_mentoring_1 && mysql -u root -p agrisense_db < add_medicines.sql
```

Then press **ENTER** when prompted for password (leave it blank).

---

## VERIFICATION COMMANDS

### Check if medicines imported
```bash
mysql -u root agrisense_db -e "SELECT COUNT(*) FROM medicines;"
```
**Expected:** `40`

### Check specific medicine
```bash
mysql -u root agrisense_db -e "SELECT name, dosage_rate, withdrawal_period_days FROM medicines WHERE name = 'Amoxycillin';"
```
**Expected:** Shows Amoxycillin with 15, 10

### Verify prescriptions saved
```bash
mysql -u root agrisense_db -e "SELECT COUNT(*) FROM prescriptions;"
```
**Expected:** Shows number of saved prescriptions

---

## TEST URLS

### Test API
```
http://localhost/update_after_mentoring_1/api/medicines.php
```

### Test Dashboard
```
http://localhost/update_after_mentoring_1/test_medicine_integration.html
```

### Main Portal
```
http://localhost/update_after_mentoring_1/index.html
```

### phpMyAdmin
```
http://localhost/phpmyadmin
```

---

## FILES TO RUN

### Option 1: Terminal (Recommended)
```bash
cd c:\xampp\htdocs\update_after_mentoring_1
mysql -u root -p agrisense_db < add_medicines.sql
```

### Option 2: Batch Script
```bash
# Double-click this file
setup_medicines.bat
```

### Option 3: phpMyAdmin
1. Go to http://localhost/phpmyadmin
2. Select database `agrisense_db`
3. Click Import
4. Choose `add_medicines.sql`
5. Click Import

---

## DOSAGE FORMULA TEST

**Input:**
- Amoxycillin: 15 mg/kg
- Animal Weight: 50 kg

**Expected Output:**
- Calculated Dosage: **750 mg**
- Calculation: 15 × 50 = 750

**Other Tests:**
- Oxytetracycline: 20 × 50 = 1000 mg
- Enrofloxacin: 10 × 50 = 500 mg
- Erythromycin: 10 × 50 = 500 mg

---

## TROUBLESHOOTING COMMANDS

### If import fails
```bash
# Check if database exists
mysql -u root -e "SHOW DATABASES;" | grep agrisense_db

# If not, create it
mysql -u root -e "CREATE DATABASE agrisense_db;"

# Then import
mysql -u root -p agrisense_db < add_medicines.sql
```

### If MySQL not found
```bash
# Navigate to MySQL bin directory
cd "C:\xampp\mysql\bin"

# Then run import
mysql -u root -p agrisense_db < "C:\xampp\htdocs\update_after_mentoring_1\add_medicines.sql"
```

### Check MySQL service
```bash
# In PowerShell as Admin
Get-Service | grep -i mysql
# Or check XAMPP Control Panel
```

---

## QUICK DEMO CHECKLIST

- [ ] Import medicines
- [ ] Open test page
- [ ] Click "Test Database" → PASS
- [ ] Click "Test API" → PASS
- [ ] Fill dropdown, click "Calculate" → 750 mg
- [ ] Open portal → Login
- [ ] Go to E-Prescriptions
- [ ] Add medicine, select one
- [ ] Verify dosage calculates
- [ ] Submit prescription
- [ ] Show success message

**Total time:** 5 minutes

---

## ESSENTIAL FILES

| File | Purpose | Action |
|------|---------|--------|
| `add_medicines.sql` | Import medicines | Run with mysql command |
| `api/medicines.php` | Serve medicines | API endpoint (auto) |
| `test_medicine_integration.html` | Test suite | Open in browser |
| `index.html` | Portal | Open in browser |
| `setup_medicines.bat` | Batch setup | Double-click |

---

## SUCCESS INDICATORS

✅ Import shows "Query OK" (no errors)  
✅ Test page: All tests PASS  
✅ API returns medicines (JSON array)  
✅ Portal dropdown shows medicines (40+)  
✅ Dosage calculates: 750 mg for (15×50)  
✅ Withdrawal shows: e.g., "10 days"  
✅ Prescription submits successfully  
✅ Database shows saved data  

---

## MOST COMMON ISSUE & FIX

**Problem:** Dropdown shows "Error loading medicines"

**Fix:**
```bash
# 1. Check medicines table
mysql -u root agrisense_db -e "SELECT COUNT(*) FROM medicines;"

# If shows 0:
# 2. Re-import
mysql -u root -p agrisense_db < add_medicines.sql

# 3. Verify API works
# Open: http://localhost/update_after_mentoring_1/api/medicines.php
```

---

## PASSWORD REMINDER

**MySQL user:** `root`  
**MySQL password:** (empty, just press ENTER)  
**XAMPP default:** No password set

---

## TIME ESTIMATES

| Task | Time |
|------|------|
| Import medicines | 1 min |
| Verify setup | 2 min |
| Test portal | 3 min |
| Demo to evaluators | 5 min |
| **Total** | **11 min** |

---

## EVALUATION DAY CHECKLIST

```
Morning:
  ✓ Import medicines
  ✓ Verify all tests pass
  ✓ Test portal works
  ✓ Review demo script

Before Evaluators Arrive:
  ✓ Portal loads without errors
  ✓ Can login successfully
  ✓ Medicine dropdown works
  ✓ Dosage calculation correct
  ✓ Can submit prescription
  ✓ Console shows no errors (F12)

During Evaluation:
  ✓ Follow demo script
  ✓ Click through features
  ✓ Explain each step
  ✓ Highlight auto-calculation
  ✓ Show database records
  ✓ Answer questions confidently
```

---

## ONE-COMMAND VERIFICATION

```bash
# This will verify everything is set up
mysql -u root agrisense_db -e "SELECT COUNT(*) as total_medicines FROM medicines; SELECT COUNT(*) as total_prescriptions FROM prescriptions; SELECT COUNT(*) as total_items FROM prescription_items;"
```

**Expected Output:**
- total_medicines: 40 (or similar)
- total_prescriptions: (number of tests you ran)
- total_items: (medicines added in prescriptions)

---

## FINAL CONFIDENCE CHECK

Run this before evaluation:

```bash
# 1. Import (if not done)
mysql -u root -p agrisense_db < add_medicines.sql

# 2. Verify
mysql -u root agrisense_db -e "SELECT COUNT(*) FROM medicines WHERE approved=1;"

# 3. Test
# Open: http://localhost/update_after_mentoring_1/test_medicine_integration.html
# Check: All tests PASS

# 4. Portal
# Open: http://localhost/update_after_mentoring_1/index.html
# Login → E-Prescriptions → Add Medicine → Verify calculation

# 5. Success
# If all above work, you're ready! ✓
```

---

**Status:** READY FOR DEPLOYMENT  
**Confidence Level:** HIGH  
**Evaluation Success Rate:** VERY HIGH  

**You've got this!** 🎉
